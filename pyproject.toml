[build-system]
requires = ["setuptools >= 64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "PyWatchdog"
version = "1.0.0"
description = "A Python Watchdog library / cli"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "wheel>=0.47.0",
]
